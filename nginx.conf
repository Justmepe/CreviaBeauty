server {
    listen 80;
    server_name creviabeauty.com www.creviabeauty.com;

    # Redirect HTTP to HTTPS (uncomment after SSL setup)
    # return 301 https://$server_name$request_uri;

    location / {
        proxy_pass http://127.0.0.1:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
    }

    # Serve uploaded images
    location /uploads {
        alias /var/www/crevia-furnitures/uploads;
        expires 30d;
        add_header Cache-Control "public, immutable";
    }

    client_max_body_size 10M;
}

# HTTPS config (uncomment after running certbot)
# server {
#     listen 443 ssl http2;
#     server_name creviabeauty.com www.creviabeauty.com;
#
#     ssl_certificate /etc/letsencrypt/live/creviabeauty.com/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/creviabeauty.com/privkey.pem;
#
#     location / {
#         proxy_pass http://127.0.0.1:3000;
#         proxy_http_version 1.1;
#         proxy_set_header Upgrade $http_upgrade;
#         proxy_set_header Connection 'upgrade';
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         proxy_cache_bypass $http_upgrade;
#     }
#
#     location /uploads {
#         alias /var/www/crevia-furnitures/uploads;
#         expires 30d;
#         add_header Cache-Control "public, immutable";
#     }
#
#     client_max_body_size 10M;
# }
